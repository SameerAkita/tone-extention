# Web app origin that hosts /connect-extension and API routes
VITE_WEB_ORIGIN=http://localhost:3000

# Optional override for rewrite endpoint
# VITE_REWRITE_ENDPOINT=http://localhost:3000/api/extension/rewrite
