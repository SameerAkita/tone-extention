import { DashboardPlaceholder } from "@/components/dashboard-placeholder";

export default function DashboardSubscriptionPage() {
  return (
    <DashboardPlaceholder
      eyebrow="Subscription"
      title="Subscription details will live here"
      description="This page can later show plan status, billing cadence, renewal timing, and Stripe management actions."
    />
  );
}
